'use client';
import {useEffect} from "react";

const Footer = () => {
    useEffect(() => {
        console.log(localStorage);
    }, []);
    return (
        <div>

        </div>
    );
};

export default Footer;